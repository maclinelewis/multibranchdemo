node {
         stage('addition') {
              sh '''
                  cd $workspace
		  print($workspace)
                  mkdir $workspace\\histogram_ref
	          pytest $file
              '''
	 
    }
}
